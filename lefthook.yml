# Lefthook configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
      stage_fixed: true

pre-push:
  commands:
    ci:
      run: npm run ci
